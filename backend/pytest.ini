[pytest]
testpaths = tests
markers =
    integration: marks tests requiring real PaddleOCR or Hardhat node
filterwarnings =
    ignore::DeprecationWarning
addopts = -m "not integration" -p no:asyncio -p no:warnings